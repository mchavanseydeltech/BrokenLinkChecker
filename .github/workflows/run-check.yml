name: Bunnings URL Checker

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:

jobs:
  run-checker:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install selenium undetected-chromedriver requests

      - name: Run Bunnings Checker
        run: python bunnings_checker.py
