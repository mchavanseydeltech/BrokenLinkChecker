name: Bunnings URL Checker

on:
  schedule:
    # Runs daily at 02:00 UTC
    - cron: '0 2 * * *'
  workflow_dispatch:  # Allows manual trigger from GitHub Actions tab

jobs:
  check_bunnings_urls:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout repo
      - name: Checkout repository
        uses: actions/checkout@v3

      # Step 2: Setup Python
      - name: Setup Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      # Step 3: Install dependencies
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install selenium undetected-chromedriver requests

      # Step 4: Run Bunnings Checker
      - name: Run Bunnings URL Checker
        run: |
          python3 bunnings_checker.py

      # Optional: Upload results as artifact (if saving CSV)
      # - name: Upload CSV results
      #   uses: actions/upload-artifact@v3
      #   with:
      #     name: bunnings-results
      #     path: bunnings_results_*.csv
